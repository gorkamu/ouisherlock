import React from 'react';
import ReactDOM from 'react-dom';
import { SherlockApp } from './SherlockApp';
import './styles/styles.scss';


ReactDOM.render(
  <React.StrictMode>
    <SherlockApp />
  </React.StrictMode>,
  document.getElementById('root')
);